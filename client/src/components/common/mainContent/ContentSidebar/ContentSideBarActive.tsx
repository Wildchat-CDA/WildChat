import React from 'react';

const ContentSideBarActive = ({ children }: { children: React.ReactNode }) => {
  return <div style={{ height: '100%' }}>{children}</div>;
};

export default ContentSideBarActive;
